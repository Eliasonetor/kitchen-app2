-- generated by cds-compiler version 2.5.2
COLUMN TABLE epam_sap_dev_schema_Orders (
  createdAt TIMESTAMP,
  createdBy NVARCHAR(255),
  modifiedAt TIMESTAMP,
  modifiedBy NVARCHAR(255),
  ID NVARCHAR(36) NOT NULL,
  toMarket_ID NVARCHAR(36),
  order_ID INTEGER,
  quantity INTEGER,
  calendarYear NVARCHAR(5000),
  deliveryDate DATE,
  orderNetAmount DECIMAL(15, 2) DEFAULT 0,
  orderTaxAmount DECIMAL(15, 2) DEFAULT 0,
  orderGrossAmount DECIMAL(15, 2) DEFAULT 0,
  currencyCode_code NVARCHAR(3),
  PRIMARY KEY(ID)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN epam_sap_dev_schema_Markets AS toMarket ON (toMarket.ID = toMarket_ID),
  MANY TO ONE JOIN sap_common_Currencies AS currencyCode ON (currencyCode.code = currencyCode_code)
)