-- generated by cds-compiler version 2.5.2
COLUMN TABLE epam_sap_dev_schema_Markets (
  createdAt TIMESTAMP,
  createdBy NVARCHAR(255),
  modifiedAt TIMESTAMP,
  modifiedBy NVARCHAR(255),
  ID NVARCHAR(36) NOT NULL,
  toProduct_ID NVARCHAR(36),
  toMarketInfos_ID INTEGER,
  startDate DATE,
  endDate DATE,
  status NVARCHAR(5000) DEFAULT 'NO',
  marketNetAmount DECIMAL(15, 2),
  marketTaxAmount DECIMAL(15, 2),
  marketGrossAmount DECIMAL(15, 2),
  marketTotalQuantity INTEGER DEFAULT 0,
  currencyCode_code NVARCHAR(3),
  identifierFieldControlMarket INTEGER DEFAULT 7,
  identifierFieldControlCalculated INTEGER DEFAULT 7,
  confirmMarketEnabled BOOLEAN NOT NULL DEFAULT FALSE,
  PRIMARY KEY(ID)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN epam_sap_dev_schema_Products AS toProduct ON (toProduct.ID = toProduct_ID),
  MANY TO ONE JOIN epam_sap_dev_masterdata_MarketInfos AS toMarketInfos ON (toMarketInfos.ID = toMarketInfos_ID),
  MANY TO ONE JOIN sap_common_Currencies AS currencyCode ON (currencyCode.code = currencyCode_code),
  MANY TO MANY JOIN epam_sap_dev_schema_Orders AS "ORDER" ON ("ORDER".toMarket_ID = ID)
)