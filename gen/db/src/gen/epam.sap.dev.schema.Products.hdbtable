-- generated by cds-compiler version 2.5.2
COLUMN TABLE epam_sap_dev_schema_Products (
  ID NVARCHAR(36) NOT NULL,
  createdAt TIMESTAMP,
  createdBy NVARCHAR(255),
  modifiedAt TIMESTAMP,
  modifiedBy NVARCHAR(255),
  product_ID NVARCHAR(20),
  toProductGroup_ID NVARCHAR(5000),
  phase_ID NVARCHAR(5000),
  height DECIMAL(13, 3),
  "DEPTH" DECIMAL(13, 3),
  width DECIMAL(13, 3),
  measure_msehi NVARCHAR(3),
  price DECIMAL(15, 2),
  currencyCode_code NVARCHAR(3),
  taxRate INTEGER,
  productNetAmount DECIMAL(15, 2) DEFAULT 0,
  productTaxAmount DECIMAL(15, 2) DEFAULT 0,
  productGrossAmount DECIMAL(15, 2) DEFAULT 0,
  productTotalQuantity INTEGER DEFAULT 0,
  PRIMARY KEY(ID)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN epam_sap_dev_masterdata_ProductGroups AS toProductGroup ON (toProductGroup.ID = toProductGroup_ID),
  MANY TO ONE JOIN epam_sap_dev_masterdata_Phases AS phase ON (phase.ID = phase_ID),
  MANY TO ONE JOIN epam_sap_dev_common_UnitOfMeasure AS measure ON (measure.msehi = measure_msehi),
  MANY TO ONE JOIN sap_common_Currencies AS currencyCode ON (currencyCode.code = currencyCode_code),
  MANY TO MANY JOIN epam_sap_dev_schema_Markets AS market ON (market.toProduct_ID = ID)
)