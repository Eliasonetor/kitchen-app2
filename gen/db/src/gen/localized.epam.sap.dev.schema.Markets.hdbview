-- generated by cds-compiler version 2.5.2
VIEW localized_epam_sap_dev_schema_Markets AS SELECT
  L.createdAt,
  L.createdBy,
  L.modifiedAt,
  L.modifiedBy,
  L.ID,
  L.toProduct_ID,
  L.toMarketInfos_ID,
  L.startDate,
  L.endDate,
  L.status,
  L.marketNetAmount,
  L.marketTaxAmount,
  L.marketGrossAmount,
  L.marketTotalQuantity,
  L.currencyCode_code,
  L.identifierFieldControlMarket,
  L.identifierFieldControlCalculated,
  L.confirmMarketEnabled
FROM epam_sap_dev_schema_Markets AS L
WITH ASSOCIATIONS (
  MANY TO ONE JOIN localized_epam_sap_dev_schema_Products AS toProduct ON (toProduct.ID = toProduct_ID),
  MANY TO ONE JOIN epam_sap_dev_masterdata_MarketInfos AS toMarketInfos ON (toMarketInfos.ID = toMarketInfos_ID),
  MANY TO ONE JOIN localized_sap_common_Currencies AS currencyCode ON (currencyCode.code = currencyCode_code),
  MANY TO MANY JOIN localized_epam_sap_dev_schema_Orders AS "ORDER" ON ("ORDER".toMarket_ID = ID)
)